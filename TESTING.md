# 실행 방법과 기능 테스트 입력값

명령은 전부 **그대로 복사해 맥 기본 터미널(zsh)에 붙여넣으면** 된다. 경로는 맨 처음 블록의 두 줄에서만 정한다.

## 1. 실행

### 1-0. 경로 설정 — 터미널을 새로 열 때마다 먼저 한 번

```bash
export HANIL_DEV="$HOME/Documents/Eugene_Group/한일합섬/dev"
export HANIL_DB="$HOME/Documents/Eugene_Group/한일합섬/DB조회도구"
export HANIL_PY="$HANIL_DB/.venv/bin/python"
```

다른 위치에 클론했거나 다른 팀원 맥이면 위 두 폴더 경로만 자기 것으로 바꾼다. 그냥 `python app.py` 로 띄우면
서버는 뜨지만 첫 조회에서 `ModuleNotFoundError: No module named 'pymssql'` 가 난다(맥 기본 파이썬에는 DB 드라이버가 없다).

### 1-1. 접속 자가점검 (처음 한 번)

```bash
"$HANIL_DB/.venv/bin/hhhs-db" check
```

"쓰기 권한 0개 · 읽기 전용" 이 나오면 정상. 실패하면 `.env`·방화벽 문제이니 hhhs-db-manager README 의 문제 해결 표를 본다.

### 1-2. 서버 실행

```bash
cd "$HANIL_DEV" && "$HANIL_PY" app.py
```

`http://127.0.0.1:8765` 를 브라우저로 연다. 이 맥에서만 열리며(127.0.0.1), 종료는 Ctrl+C.
`Address already in use` 가 나면 이전 서버가 살아 있는 것이다. 다른 터미널에서 끄고 다시 띄운다:

```bash
lsof -ti :8765 | xargs kill
```

### 1-3. DB 없이 로직만 점검

```bash
cd "$HANIL_DEV" && "$HANIL_PY" test_order_check.py
```

모든 줄이 `ok test_…` 로 나오면 정상(오류가 있으면 Traceback 이 찍힌다).

### 1-4. 자주 나는 오류

| 메시지                                                     | 원인                                                 | 조치                                                                                      |
| ---------------------------------------------------------- | ---------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| `ModuleNotFoundError: No module named 'pymssql'`         | hhhs-db-manager 가 없는 파이썬(anaconda 등)으로 띄움 | 1-0 의`$HANIL_PY` 로 다시 실행. 또는 `pip install -e "$HANIL_DB"` 로 내 파이썬에 설치 |
| `ModuleNotFoundError: No module named 'hhhs_db_manager'` | 저장소가`DB조회도구/` 옆이 아님                    | `HHHS_DB_DIR="$HANIL_DB" "$HANIL_PY" app.py`                                            |
| `Address already in use`                                 | 8765 포트에 이전 서버가 떠 있음                      | `lsof -ti :8765 \| xargs kill`                                                           |
| 화면에`… MA_PITEM(의령 3000)에 없는 품목코드입니다`     | 코드 오타 또는 붙여넣기에 섞인 보이지 않는 문자      | 자동완성 목록에서 고른다. 메시지에 받은 값과 글자 수가 그대로 찍힌다                      |
| 화면에`DBError …`                                       | ERP 접속·권한·제한시간                             | 1-1 자가점검                                                                              |

한 조회에 ERP 쿼리 5회(기초행 확인 · 품목 · 재고 · 미출하 수주 · 생산요청)가 나가며 보통 2~3초 걸린다.

## 2. 화면 사용 순서

1. 품목코드에 `2PD` 처럼 두 글자 이상 치면 코드·품명·평량·색상 자동완성 목록이 뜬다.
2. 폭(mm)·길이(m)·등급·롤수를 넣고 **판단하기**. 등급을 비우면 A. 롤수와 kg 는 한쪽만 넣으면 다른 쪽이 자동 계산된다(마지막에 고친 값 기준).
3. 결과: 배분 막대(파랑 재고출하 · 하늘 대체검토 · 보라 생산의뢰), 3개 타일, 현재고−미출하=가용, 대체 후보 표(배정 행 강조), 미출하 수주, 최근 생산요청.
4. 왼쪽 아래 **거래처 대체 기준** 카드에서 폭 허용(+mm)·길이 허용(+m)·허용 품목·허용 등급·코멘트를 적는다. 기본은 폭 +200mm · 길이 +200m.
   - **이 주문에만 적용**: 저장하지 않고 지금 주문만 그 기준으로 다시 판단한다(거래처코드 불필요). 결과 배지가 "임시 기준 · 저장 안 됨"으로 표시된다. 이후 "판단하기"를 누르면 임시 기준은 버려진다.
   - **저장 후 다시 판단**: 거래처코드가 있어야 활성화된다(기준이 거래처별로 `substitute_rules.json` 에 저장되므로). 저장하면 그 거래처의 다음 조회부터 계속 적용된다.

## 3. 테스트 입력값

재고는 매일 바뀌므로 아래 "기대 결과"는 **모양**을 적은 것이다. 숫자는 그날의 ERP 값을 따른다.
품목코드 규칙: `2PD2040NT1N` = 2호기 · PP · 엠보 D2 · 40 g/㎡ · NATURAL · 기능 N(일반). 첫 자리 1 = 1호기(PET/PLA), 2 = 2호기(PP).

| #   | 목적                     | 품목            | 폭   | 길이 | 등급 | 수량        | 거래처            | 기대 결과                                                                                                                                                                                                                                 |
| --- | ------------------------ | --------------- | ---- | ---- | ---- | ----------- | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1  | 세 경로 동시 배분        | `2PD2040NT1N` | 1070 | 2000 | A    | 48롤        | —                | "예시 채우기" 버튼 값. 재고출하 + 대체검토(1100폭 슬리팅 후보 등) + 생산의뢰가 함께 나오는 날이 많다. 막대가 3색으로 나뉘는지, 타일 비율 합이 100%인지 확인                                                                               |
| T2  | 전량 재고출하            | `1TC2030NT2C` | 670  | 2000 | A    | 2롤         | —                | 이 규격은 재고가 많은 편. 판단 배지가 재고출하 하나 + "전량 재고 출하 가능" 초록 배지                                                                                                                                                     |
| T3  | 재고 초과 → 대체 + 생산 | `2PD2030WH2N` | 1000 | 1000 | A    | 60롤        | —                | 가용을 넘는 만큼 대체 후보(1050·1090폭, 같은 폭의 더 긴 롤 등)에 손실 적은 순으로 배정, 그래도 남으면 생산의뢰. 후보 구분 열에 "폭+50"·"길이+…" 표시. 동일규격 미출하 수주가 있으면 가용에서 빠진 것이 표에 보인다                     |
| T4  | 거래처 기준 적용         | T3 과 동일      |      |      |      |             | 임의 코드`TEST` | 거래처 카드에서 폭 허용을`300`, 길이 허용을 `1000` 으로 저장 → 1300폭·2000m 등 후보가 추가돼 생산의뢰가 줄거나 0. 카드 배지가 "거래처 TEST"로 바뀌고 저장일이 붙는다. 시험 후 `substitute_rules.json` 에서 `TEST` 항목을 지운다 |
| T5  | 허용 등급·품목          | `2PD2040NT1N` | 1070 | 2000 | A    | 48롤        | `TEST`          | 허용 등급에`A1`, 허용 품목에 `2PD2040NT1B` 를 넣고 저장 → 후보 표 구분 열에 "등급"·"품목" 표시가 생긴다(해당 재고가 있을 때)                                                                                                        |
| T5b | 임시 기준 (저장 없이)    | T1 과 동일      |      |      |      |             | (비움)            | 예시 채우기 → 판단하기 → 카드에 허용 등급`A0, A1` 입력 → **이 주문에만 적용**. 후보 배지가 "임시 기준 · 저장 안 됨", `substitute_rules.json` 은 그대로. 다시 "판단하기"를 누르면 "기본 기준"으로 돌아온다                   |
| T6  | kg 입력 → 롤수 자동     | `2PD2038WH1N` | 650  | 2000 | A    | kg`592.8` | —                | kg 를 치면 롤수 칸이 12로 자동 채워진다(롤당 49.4 kg, 올림). 반대로 T1 에서 롤수 48을 치면 kg 칸이 4108.8 로 채워진다. 입력칸 아래에 롤당 kg 와 평량이 표시된다                                                                           |
| T7  | 등급 키 확인             | `1TC2030NT2C` | 670  | 2000 | A1   | 2롤         | —                | A 와 A1 은 다른 재고. T2 와 현재고 숫자가 달라야 한다                                                                                                                                                                                     |
| T8  | 범위 밖 품목             | `3PD2060UB1N` | 600  | 200  | A    | 1롤         | —                | 빨간 오류 배너: "1·2호기 품목(코드 첫 자리 1/2)만 지원합니다."                                                                                                                                                                           |
| T9  | 입력 누락                | `2PD2040NT1N` | 1070 | 2000 | A    | (비움)      | —                | 오류 배너: 롤수 또는 kg 필요                                                                                                                                                                                                              |
| T10 | 없는 품목                | `2PD9999XX9X` | 1000 | 1000 | A    | 1롤         | —                | 오류 배너: MA_PITEM 에 없는 품목                                                                                                                                                                                                          |

CLI 로 같은 것을 볼 때 (1-0 의 경로 설정이 된 터미널에서):

```bash
cd "$HANIL_DEV" && "$HANIL_PY" order_check.py 2PD2040NT1N -w 1070 -l 2000 -g A -r 48
```

```bash
cd "$HANIL_DEV" && "$HANIL_PY" order_check.py 2PD2030WH2N -w 1000 -l 1000 -g A -r 60
```

```bash
cd "$HANIL_DEV" && "$HANIL_PY" order_check.py 2PD2038WH1N -w 650 -l 2000 --kg 592.8
```

API 로 볼 때 (서버가 떠 있는 상태에서, 아무 터미널):

```bash
curl -s "http://127.0.0.1:8765/api/check?item=2PD2040NT1N&width=1070&length=2000&grade=A&rolls=48" | python3 -m json.tool | head -40
```

```bash
curl -s "http://127.0.0.1:8765/api/check?item=2PD2040NT1N&width=1070&length=2000&grade=A&rolls=48&temp=1&grades=A0,A1&width_plus=300&length_plus=500" | python3 -m json.tool | grep -E '"(rule_source|stock_rolls|sub_rolls|prod_rolls)"'
```

```bash
curl -s "http://127.0.0.1:8765/api/items?q=2PD2040" | python3 -m json.tool
```

```bash
curl -s -X POST http://127.0.0.1:8765/api/rules -H "Content-Type: application/json" -d '{"partner":"TEST","width_plus":300,"length_plus":1000,"grades":"A1","note":"시험"}'
```

시험 뒤 TEST 거래처 기준 삭제 (파일에서 지운다):

```bash
cd "$HANIL_DEV" && python3 -c "import json;p='substitute_rules.json';r=json.load(open(p));r.pop('TEST',None);json.dump(r,open(p,'w'),ensure_ascii=False,indent=2);print(r)"
```

## 4. 확인 포인트

- **현재고 대조**: ERP 「수주등록(SB)」 화면의 현재고 롤수/KG 와 "현재고 동일규격" 값이 같아야 한다(회사·공장·창고 3000·품목·폭·길이·등급 기준).
- **가용 = 현재고 − 미출하 수주**: ERP 화면에는 없는 값. 미출하 수주 표의 잔량 kg 을 롤당 kg 으로 나눈 만큼 빠진다.
- **후보 순서**: 같은 품목 → 같은 등급 → 좁은 폭 → 짧은 길이. 배정 열이 위에서부터 채워지는지.
- **비율 합**: 재고출하 + 대체검토 + 생산의뢰 = 주문 롤수.
- **연초**(1월~2월 중순): 결과 하단 "원장 YYYY0000 이후"가 전년으로 표시되면 정상(올해 기초행이 아직 없을 때 자동 전환).
